{
  "name": "anyaparallax",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check:routes": "node scripts/check-routes.mjs",
    "check:data": "node --experimental-strip-types --no-warnings scripts/run-checks.mjs",
    "check:images": "node scripts/generate-dev-images.mjs --check",
    "check:fixtures": "node scripts/check-upload-fixtures.mjs",
    "check:platform": "node scripts/checks/check-image-platform.mjs",
    "check:metadata": "node scripts/checks/check-served-metadata.mjs",
    "check:enquiries": "node scripts/checks/check-slice08-served.mjs",
    "check:photos": "node scripts/checks/check-photo-management-served.mjs",
    "check:csrf": "node scripts/checks/check-admin-csrf-served.mjs",
    "check:preview": "node scripts/checks/check-preview-notices.mjs",
    "check:readiness": "node scripts/checks/check-publication-readiness-served.mjs",
    "check:served": "node scripts/check-served-payload.mjs",
    "precheck": "pnpm run build && pnpm run check:fixtures",
    "check": "pnpm run typecheck && pnpm run check:routes && pnpm run check:data && pnpm run check:images && pnpm run check:platform && pnpm run check:metadata && pnpm run check:enquiries && pnpm run check:photos && pnpm run check:csrf && pnpm run check:preview && pnpm run check:readiness && pnpm run check:served",
    "db:migrate:local": "wrangler d1 migrations apply anyaparallax --local",
    "db:migrate:remote": "wrangler d1 migrations apply anyaparallax --remote",
    "db:seed:local": "node --experimental-strip-types --no-warnings scripts/seed-local-d1.mjs",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "react-router": "8.4.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.56.0",
    "@react-router/dev": "8.4.0",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "typescript": "7.0.2",
    "vite": "8.3.0",
    "wrangler": "4.135.0"
  }
}
